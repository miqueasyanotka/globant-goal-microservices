/*
package com.microservice.qualification.repository;

import com.microservice.qualification.entity.Qualification;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.mongodb.repository.MongoRepository;

import java.util.List;

public interface QualificationRepository extends MongoRepository<Qualification, String> {

    List<Qualification> findByUserId(String userId);

    List<Qualification> findByRestaurantId(String restaurantId);
}

*/
